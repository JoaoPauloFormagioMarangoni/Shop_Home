import styled from 'styled-components'

export const ShareYourSetupContainer = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;

  p {
    color: #616161;
  }

  h3 {
    font-size: 2rem;
    margin-bottom: 20px;
    color: var(--text);
  }

  img {
    width: 100%;
    height: 700px;
    object-fit: cover;
  }
`
